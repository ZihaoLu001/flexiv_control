---
name: Bug report
about: Report a problem with flexiv_control
title: "[bug] "
labels: bug
---

**Describe the bug**
A clear description of what went wrong.

**To reproduce**
Minimal steps / code. Note the **backend** (`fake` / `mujoco` / `flexiv_rdk`).

```python
# minimal repro
```

**Expected behavior**
What you expected instead.

**Environment**
- flexiv_control version: `python -c "import flexiv_control as fc; print(fc.__version__)"`
- Python version / OS:
- Backend + (if real) Flexiv RDK version and robot software version:

**Safety**
If this involved real hardware, describe the `SafetyProfile` in use and whether a
protective stop / fault occurred.
